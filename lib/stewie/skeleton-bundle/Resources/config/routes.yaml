stefanwiegmann_skeleton:
    resource: "@StewieSkeletonBundle/Controller/"
    type:     annotation
    # prefix:   "{_locale}/"
    # defaults:
    #   _locale: en
    # requirements:
    #   _locale: en|de
